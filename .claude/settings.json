{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"D:/dev/serentaeri\" && git pull --rebase --autostash origin master >/dev/null 2>&1 || true",
            "timeout": 30,
            "statusMessage": "Auto git pull from GitHub..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"D:/dev/serentaeri\" && git add -A && (git diff --cached --quiet || git commit -m \"auto: $(date +%Y-%m-%d\\ %H:%M:%S)\" >/dev/null 2>&1) || true",
            "timeout": 30,
            "statusMessage": "Auto commit (no push)..."
          }
        ]
      }
    ]
  }
}
